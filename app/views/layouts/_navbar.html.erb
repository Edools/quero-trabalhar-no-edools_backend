<nav class="navbar navbar-default navbar-configuration">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#theChallenge" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand">The Challenge</div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    
    <div class="collapse navbar-collapse" id="theChallenge">
      <ul class="nav navbar-nav">
        <li> <%= link_to 'Perfil',pages_index_path %></li>

        <% if current_user.type_profile.downcase == 'school' %>
          <% if current_user.school %>
            <li> <%= link_to 'Administrar Cursos', school_path(current_user.school) %></li>
          <%else%>
            <li> <%= link_to 'Criar Instituição', new_school_path%></li>
          <%end%>
        <%else%>
          <%if current_user.student %>
            <li> <%= link_to 'Explorar Cursos', pages_discovery_path %></li>
          <%else%>
            <li> <%= link_to 'Finalize seu cadastro', new_student_path%></li>
          <%end%>
        <%end%> <!-- END-Check if type is School -->





      </ul>




      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Logout", destroy_user_session_path, :method => :delete%></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>