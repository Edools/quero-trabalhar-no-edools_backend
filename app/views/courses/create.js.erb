<% if @course.errors.any? %>
  $('#main-modal').html("<%= escape_javascript(render 'new')%>");
  $('.datepicker').datepicker();
<% else %>
  $('#courses').html("<%= escape_javascript(render 'courses/shared/courses', courses: @school.courses) %>")
  $('#main-modal').modal('close');
<% end %>
