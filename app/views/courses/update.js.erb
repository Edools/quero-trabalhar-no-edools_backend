<% if @course.errors.any? %>
  $('#main-modal').html("<%= escape_javascript(render 'edit')%>");
  $('.datepicker').datepicker();
<% else %>
  $('#show').html("<%= escape_javascript(render 'show', courses: @school.courses) %>")
  $('#main-modal').modal('close');
<% end %>
