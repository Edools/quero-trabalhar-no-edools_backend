<%= render 'shared/notice' %>
<div class="row">
  <div class="col-md-12">
    <div class="row">
      <div class="col-md-8 pull-left">
        <%= link_to t('actions.back'), relatorios_path, class: 'btn btn-default' %>
      </div>
    </div>
  </div>
</div>
<h1>Relatórios</h1>
<div class="row">
  <div class="col-md-12">
    <table class="table table-hover">
      <thead>
      <tr>
        <th>Nome</th>

        <th colspan="3"></th>
      </tr>
      </thead>
      <tbody>
      <% @escola.alunos.where(ativo: true).each do |aluno| %>
          <tr>
            <td><%= aluno.nome %></td>
            <td><%= link_to t('actions.show'), aluno %></td>
            <td><%= link_to t('actions.edit'), edit_aluno_path(aluno) %></td>
          </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>
